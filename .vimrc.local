"soruby
"colorscheme soruby
"set guifont=HackNF-Regular:h18
"set guifont=HackNFM-Regular:h18
"set guifont=HackNFP-Regular:h18

set encoding=utf-8
" *'termencoding'* *'tenc'* (Vim 7.4.852 also removed this for Windows)
"set termencoding=utf-8
if !has("nvim")
    set guifont=Menlo:h16
    colorscheme onedark
    set fileencoding=utf-8
    nmap <F1> :tabnew 
    nmap <F2> :tabprevious<CR>
    nmap <F3> :tabnext<CR>
    "let &t_SI = "\<Esc>]50;CursorShape=1\x7"
    "let &t_SR = "\<Esc>]50;CursorShape=2\x7"
    "let &t_EI = "\<Esc>]50;CursorShape=0\x7"
    let &t_SI = "\<Esc>Ptmux;\<Esc>\<Esc>]50;CursorShape=1\x7\<Esc>\\"
    let &t_SR = "\<Esc>Ptmux;\<Esc>\<Esc>]50;CursorShape=2\x7\<Esc>\\"
    let &t_EI = "\<Esc>Ptmux;\<Esc>\<Esc>]50;CursorShape=0\x7\<Esc>\\"
endif
set fileencodings=UCS-bom,utf-8,gb18030,gbk,latin1

set ignorecase
set smartcase
set nospell
set nrformats=bin
set relativenumber
" 加快宏的办法
set lazyredraw
" normal! 10@a

language messages zh_CN.utf-8


au BufNewFile,BufRead *.groovy  setf groovy

" let mapleader = "\<Space>"
" let g:mapleader = "\<Space>"
" let g:C_MapLeader="\<Space>"

if !has("nvim") && has("gui_running")
    "添加水平滚动条
    set guioptions+=b
endif

"自动保存文件
"set autowriteall

"hi cursorline guifg=magenta cyan orange purple violet seagreen slateblue darkred

"json文件格式化
nnoremap <unique><silent> <localleader>fj :call FormatJson()<cr>
"xml文件格式化，需要libxml2支持，在百度云盘中有
nnoremap <unique><silent> <localleader>fx :call FormatXml()<cr>
nnoremap <unique><silent> <localleader>xi :%!xmllint --encode utf8 --pretty 1 --recover - 2>/dev/null<cr>
vnoremap <unique><silent> <localleader>xi :!xmllint --encode utf8 --pretty 1 --recover - 2>/dev/null<cr>
vnoremap <unique><silent> <localleader>fp :call FormatPrivate()<cr>
nnoremap <unique><silent> <localleader>fp :call FormatPrivate()<cr>
vnoremap <unique><silent> <localleader>fr :call FormatResponse()<cr>
vnoremap <unique><silent> <localleader>fm :call FormatMapper()<cr>
vnoremap <unique><silent> <localleader>fw :call DelChineseWidth()<cr>

" :help indent-guides
nmap <silent> <Leader>ig <Plug>IndentGuidesToggle
nmap <silent> <Leader>ie <Plug>IndentGuidesEnable
nmap <silent> <Leader>id <Plug>IndentGuidesDisable

" 编号自动更正
nnoremap <unique><silent> <leader>fn :call UpdateNumberedList()<cr>

" 行列转换
"nnoremap <leader>f\| :call Transpose("\|")<CR>:Tabularize /\|<CR>
"vnoremap <leader>f\| :call Transpose("\|")<CR>
"nnoremap <leader>f, :call Transpose(",")<CR>:Tabularize /,<CR>
"vnoremap <leader>f, :call Transpose(",")<CR>
"nnoremap <leader>ft :call Transpose("\t")<CR>:Tabularize /\t<CR>
"vnoremap <leader>ft :call Transpose("\t")<CR>
"nnoremap <leader>f: :call Transpose(":")<CR>:Tabularize /:<CR>
"vnoremap <leader>f: :call Transpose(":")<CR>
nnoremap <leader>f :call TransposeAndTabularize(0)<CR>
vnoremap <leader>f :<C-u>call TransposeAndTabularize(1)<CR>

vnoremap <leader>uc :!uniq -c \| awk '$1>1 {print $0}'<CR>

nnoremap <leader>st vip:s/\t/,/g<CR>
vnoremap <leader>st :s/\t/,/g<CR>
nnoremap <leader>su vip:s/,/\t/g<CR>
vnoremap <leader>su :s/,/\t/g<CR>

" 表格格式转为markdown的表格 Tableize
vnoremap <silent> <leader>ta :call Tableize()<cr>:call MaybeRestoreVisual()<cr>

" 列模式转换所在位置
let @g='omg:let g=col(".")gvo:exec "normal! ".g."|"`go'

" 向下移动直到列宽不足当前光标，
" 必须要用<Cmd>不然在命令行模式下得到的列宽为0（:的命令行改变了光标位置）
" 向下
nnoremap <silent> g] :<C-u>call MoveToLastValidColumn('down')<CR>
xnoremap <silent> g] <Cmd>call MoveToLastValidColumn('down')<CR>
onoremap <silent> g] <Cmd>call MoveToLastValidColumn('down')<CR>

" 向上
nnoremap <silent> g[ :<C-u>call MoveToLastValidColumn('up')<CR>
xnoremap <silent> g[ <Cmd>call MoveToLastValidColumn('up')<CR>
onoremap <silent> g[ <Cmd>call MoveToLastValidColumn('up')<CR>

"'<,'>!awk '/alter table/ {gsub(/[0-9]{8}/, sprintf("\%08d", count)); count++; print; next} {print}'
vnoremap <leader>nf :!awk '/alter table/ {gsub(/[0-9]{8}/, sprintf("\%08d", count)); count++; print; next} {print}'
nnoremap <leader>nf :%!awk '/alter table/ {gsub(/[0-9]{8}/, sprintf("\%08d", count)); count++; print; next} {print}'

" /ALTER TABLE/ {
"     # gsub(/[0-9]{8}/, sprintf("%08d", count++))
"     # gsub(/[0-9]{8}/, sprintf("%02d%06d", (1+(count/3)), count++%3))
"     gsub(/[0-9]{8}/, sprintf("%08d", strftime("%Y%m%d", mktime("2022 01 01 00 00 00") + (count++ * 86400))))
"     print
"     next
" }
" {
"     print
" }
"
" !gawk '/TABLE/ {gsub(/[0-9]{8}_[0-9]{8}/, sprintf("\%08d_\%08d", strftime("\%Y\%m\%d", mktime("2022 01 01 00 00 00") + (count/3 * 86400)),count++\%3));print;next}{print}'


"autocmd InsertEnter * execute "silent !macism 'im.rime.inputmethod.Squirrel.Hans' > /dev/null"
"autocmd InsertLeave * execute "silent !macism 'com.apple.keylayout.ABC' > /dev/null"


function! TransposeAndTabularize(is_visual)
    " 获取用户输入的字符
    echo "Enter separator: "
    let char = nr2char(getchar())
    redraw
    
    " 处理特殊字符的转义
    let escaped = escape(char, '\|')
    
    call Transpose(char)
    
    if !a:is_visual
        exe "Tabularize /" . escaped
    endif
endfunction


function! Transpose(includepat) range
    let top = a:firstline
    let bot = a:lastline
    if a:includepat != '' && top == bot
        if top < 0 || top > line('$') || getline(top) !~ a:includepat
            return
        endif
        while top > 1 && getline(top-1) =~ a:includepat
            let top -= 1
        endwhile
        while bot < line('$') && getline(bot+1) =~ a:includepat
            let bot += 1
        endwhile
    endif
    let range = top . ',' . bot
    if a:includepat == ''
        let command="!gawk '{for(i=1;i<=NF;i++) a[i,NR]=$i}END{for(i=1;i<=NF;i++) {for(j=1;j<=NR;j++) printf a[i,j] ;print \"\"}}'"
    else
        let command="!gawk -F'" . a:includepat . "' '{for(i=1;i<=NF;i++) a[i,NR]=$i}END{for(i=1;i<=NF;i++) {for(j=1;j<=NR;j++) printf a[i,j] \"" . a:includepat . "\";print \"\"}}'"
    endif
    exe range . command
    exec
endfunction


fun! UpdateNumberedList()
  let lnum       = line('.')
  let num_pat    = '^>\? *\d\.[0-9.]*'
  let result     = []
  let init_depth = -1

  while (indent(lnum) > 0 && prevnonblank(lnum - 1) == lnum - 1)
    let lnum -= 1
  endwhile

  if (match(getline(lnum), num_pat) > -1)
    let start_lnum  = lnum
    let end_lnum    = lnum
    let indent_size = shiftwidth()

    while (nextnonblank(start_lnum - 1) == start_lnum - 1)
      let start_lnum -= 1
    endwhile

    while (nextnonblank(end_lnum + 1) == end_lnum + 1)
      let end_lnum += 1
    endwhile

    for lnum in range(start_lnum, end_lnum)
      let raw_line = getline(lnum)
      let line     = substitute(raw_line, '^ *>', '', '')
      let depth    = len(get(matchlist(line, '^ *'), 0, '')) / indent_size
      let line     = substitute(line, '^ *', '', '')
      let prev     = get(result, -1)
      let has_prev = type(prev) == type({})
      let nums     = []

      if init_depth == -1
        let init_depth = depth
      endif

      let idx_depth = depth - init_depth

      if (match(line, num_pat) > -1)
        if (has_prev)
          let idx_depth = min([idx_depth, prev.idx_depth + 1])
          let depth = min([depth, prev.depth + 1])
          let nums  = copy(prev.nums)

          if idx_depth > prev.idx_depth
            call add(nums, 1)
          elseif idx_depth < prev.idx_depth
            let nums = nums[0:idx_depth]
            let nums[idx_depth] += 1
          elseif idx_depth == prev.idx_depth
            let nums[idx_depth] += 1
          endif
        else
          call add(nums, 1)
        endif

        call add(result, {
              \ 'quoted': match(raw_line, '^ *>') > -1,
              \ 'lnum': lnum,
              \ 'depth': depth,
              \ 'idx_depth': idx_depth,
              \ 'lines': [line],
              \ 'nums': nums
              \ })
      elseif (has_prev)
        call add(prev.lines, line)
      endif
    endfor
  endif

  for item in result
    let indent = repeat(' ', indent_size * item.depth)
    let quote  = item.quoted ? '> ' : ''
    let lnum   = item.lnum
    let line   = substitute(item.lines[0], num_pat, join(item.nums, '.') . '.', '')

    call setline(lnum, quote . indent . line)

    if (len(item.lines) > 1)
      let sp_pat        = '^>\? *\(\([0-9.]\+\|[' . join(g:mkdx#settings.tokens.enter, '') . ']\)\( \[.\]\)\? \|\[.\]\)'
      let item_syms     = get(matchlist(line, sp_pat), 1, '')
      let inline_indent = repeat(' ', len(item_syms))

      for inline_line in item.lines[1:]
        let lnum += 1
        call setline(lnum, quote . indent . inline_indent . inline_line)
      endfor
    endif
  endfor

  return result
endfun

" :help insert.txt
" :help usr_24.txt
"inoremap <C-A> <Home>
"inoremap <C-E> <End>
"inoremap <C-B> <Left>
"inoremap <C-J> <Down>
"inoremap <C-K> <Up>
"inoremap <C-F> <Right>

" TRADER_NAME	商户名称
" -->
" // 商户名称
" @JsonProperty("TRADER_NAME")
" private String traderName;
" 0f	d$O// pV:s/\t//gjyypkI@JsonProperty("lxxA")jVuV:s/_\(.\)/\u\1/geIprivate String A;j0

" active_time	生效时间
" -->
" // 生效时间
" @Column(name = "active_time")
" private String activeTime;
" 0f	r d$O//pjyypV:s/_\([a-z]\)/\u\1/geki@Column(krkbname = "krkbA")jIprivate String A;j
nnoremap <silent> <F4> :NvimTreeToggle<CR>
nnoremap <F5> :UndotreeToggle<cr>
"inoremap jk <esc>
vnoremap . :normal! .<CR>
"nnoremap <leader>it :silent !env VIMRUNTIME="" VIM="" open -a iTerm .<CR>
nnoremap <leader>it :silent !env VIMRUNTIME="" VIM="" open -a iTerm "<C-R>=expand('%:p:h')<CR>"<CR>

" smartim输入法切换配置 begin
" see https://cloud.tencent.com/developer/article/1877309
" see https://blog.csdn.net/lxyoucan/article/details/114365153
let g:smartim_default = 'com.apple.keylayout.ABC'
inoremap <C-c> <esc>
"临时禁用输入法切换插件，可以极大提升宏运行的效率
nnoremap <silent><leader>s0 <cmd>let g:smartim_disable = 1<CR>
"开启输入法切换插件
nnoremap <silent><leader>s1 <cmd>let g:smartim_disable = 0<CR>
" smartim输入法切换配置 end

"airblade/vim-gitgutter 插件 在git文件中自动显示修改
"set updatetime=250
let g:gitgutter_enabled = 0
"Xuyuanp/nerdtree-git-plugin 插件，在NERDTree插件中显示git文件状态
let g:NERDTreeGitStatusIndicatorMapCustom = {
    \ "Modified"  : "*",
    \ "Staged"    : "+",
    \ "Untracked" : "☆",
    \ "Renamed"   : "→",
    \ "Unmerged"  : "═",
    \ "Deleted"   : "×",
    \ "Dirty"     : "※",
    \ "Clean"     : "√",
    \ "Unknown"   : "?"
    \ }

"Rainbow 对xml格式化有影响
"let g:rainbow_active = 0
"let g:rainbow_conf = {
"    \   'guifgs': ['royalblue3', 'darkorange3', 'seagreen3', 'firebrick'],
"    \   'ctermfgs': ['lightblue', 'lightyellow', 'lightcyan', 'lightmagenta'],
"    \   'operators': '_,_',
"    \   'parentheses': ['start=/(/ end=/)/ fold', 'start=/\[/ end=/\]/ fold', 'start=/{/ end=/}/ fold'],
"    \   'separately': {
"    \       '*': {},
"    \       'tex': {
"    \           'parentheses': ['start=/(/ end=/)/', 'start=/\[/ end=/\]/'],
"    \       },
"    \       'lisp': {
"    \           'guifgs': ['royalblue3', 'darkorange3', 'seagreen3', 'firebrick', 'darkorchid3'],
"    \       },
"    \       'vim': {
"    \           'parentheses': ['start=/(/ end=/)/', 'start=/\[/ end=/\]/', 'start=/{/ end=/}/ fold', 'start=/(/ end=/)/ containedin=vimFuncBody', 'start=/\[/ end=/\]/ containedin=vimFuncBody', 'start=/{/ end=/}/ fold containedin=vimFuncBody'],
"    \       },
"    \       'html': {
"    \           'parentheses': ['start=/\v\<((area|base|br|col|embed|hr|img|input|keygen|link|menuitem|meta|param|source|track|wbr)[ >])@!\z([-_:a-zA-Z0-9]+)(\s+[-_:a-zA-Z0-9]+(\=("[^"]*"|'."'".'[^'."'".']*'."'".'|[^ '."'".'"><=`]*))?)*\>/ end=#</\z1># fold'],
"    \       },
"    \       'css': 0,
"    \   }
"    \}


function FormatXml() range
    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "<aaa><cr><bbb>
    exec l:firstline . "," . l:lastline . 's/<[-_:A-z0-9]\+>\(\s*<[-_:A-z0-9]\+>\)\@=/&\r/ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "</aaa><cr>
    exec l:firstline . "," . l:lastline . 's,</[-_:A-z0-9]\+>\s*\($\)\@!,&\r,ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "><cr><aaa/>
    exec l:firstline . "," . l:lastline . 's,\(>\)\@<=\s*<[-_:A-z0-9]\+/>,\r&,ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "<aaa/><cr><
    exec l:firstline . "," . l:lastline . 's,<[-_:A-z0-9]\+/>\s*\(<\)\@=,&\r,ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "使用xmllint格式化
    exec l:firstline . "," . l:lastline . '!xmllint --encode utf8 --pretty 1 --recover - 2>/dev/null'
endfunction

function FormatJson() range
    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    ", => ,<cr>
    exec l:firstline . "," . l:lastline . 's/,\($\)\@!/,\r/ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "key=value] => key=value<cr>]
    exec l:firstline . "," . l:lastline . 's/^[^{\[\]}]\+\([\]}]\)\@=/&\r/ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "^[... => [<cr>]
    exec l:firstline . "," . l:lastline . 's/^[{\[]\([}\],$]\)\@!/&\r/ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "...[... => ...[<cr>
    exec l:firstline . "," . l:lastline . 'g/[^\]}]/s/[^\[{][\[{]\([}\]$]\)\@!/&\r/ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "[[... => [<cr>[
    exec l:firstline . "," . l:lastline . 's/[\[{][^\[{]*\([\[{]\)\@=/&\r/ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "]] => ]<cr>]
    exec l:firstline . "," . l:lastline . 's/[\]}][^\]}]*\([\]}]\)\@=/&\r/ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    "[..., => [<cr>...,
    exec l:firstline . "," . l:lastline . 's/\([\[{]\)\@<=[^\]}]*,\@=/\r&/ge'

"    " 起括号在同一行还是换行
"    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
"    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
"    if l:lastline < 0
"        let l:lastline = line('$')
"    endif
"    "=[ => =<cr>[
"    exec l:firstline . "," . l:lastline . 's/\([:=]\)\([\[{]$\)/\1\r\2/ge'

    " Locate block of code to be considered (same indentation, no blanks)
    let l:firstline  = search('^\%(.\)\@!','bnW') + 1
    let l:lastline   = search('^\%(.\)\@!', 'nW') - 1
    if l:lastline < 0
        let l:lastline = line('$')
    endif
    let l:centline = l:lastline - l:firstline
    exec l:firstline
    let l:save_ft = &filetype
    set filetype=json
    exec "normal " . l:centline . "=="
    let &filetype=l:save_ft
endfunction



function FormatPrivate() range
    let top = a:firstline
    let size = a:lastline - a:firstline + 1
    while size > 0
        exe top . "," . top . 'normal 0/[ \t]d$O//p0llr jVuV:s/_\([a-z]\)/\u\1/egIprivate String A;j0'
        let size -= 1
        let top += 2
    endwhile
endfunction


function FormatResponse() range
    let top = a:firstline
    let size = a:lastline - a:firstline + 1
    while size > 0
        exe top . "," . top . 'normal yypkVuV:s/_\(.\)/\u\1/egIthis.A = (StringJacontent.get(lxi"lxA");j'
        let size -= 1
        let top += 1
    endwhile
endfunction

function FormatMapper() range
    let top = a:firstline
    let size = a:lastline - a:firstline + 1
    while size > 0
        exe top . "," . top . 'normal yypkVuV:s/_\(.\)/\u\1/egI<rule source="lxA" target="lxJxA"/>j'
        let size -= 1
        let top += 1
    endwhile
endfunction

" add by xcz 2021.2.21
" 修复Monodraw软件生成的图形中文偏移问题
" 删除中文之后的多余字符，因为unicode编码中文是3字节，宽度却是2字节，导致偏移
function DelChineseWidth() range
    let s:top = a:firstline
    let s:tail = a:lastline
    execute "normal \<ESC>:" .. s:top .. "\<cr>"
    let s:begin = getpos('.')
    execute "normal /[\u4e00-\u9fa5]\<cr>"
    while line('.') <= s:tail
        let s:c_str=matchstr(getline('.')[getcurpos()[2]-1:],'[\u4e00-\u9fa5]\+')
        exec "normal " .. strchars(s:c_str) .. "l"
        exec "normal " .. (strwidth(s:c_str)-strchars(s:c_str)) .. "x"
        let s:pre_pos = getpos('.')
        execute "normal /[\u4e00-\u9fa5]\<cr>"
        let s:now_pos = getpos('.')
        if s:now_pos[1] < s:pre_pos[1]
            break
        elseif s:now_pos[1] == s:pre_pos[1] && s:now_pos[2] < s:pre_pos[2]
            break
        endif
    endwhile
    call setpos('.', s:begin)
endfunction


function! ToggleIsNot()
    let l:word = expand('<cword>')
    if l:word[0:4] ==# 'isNot'
        let l:new_word = 'is' . l:word[5:]
    elseif l:word[0:1] ==# 'is'
        let l:new_word = 'isNot' . l:word[2:]
    elseif l:word == 'true'
        let l:new_word = 'false'
    elseif l:word == 'false'
        let l:new_word = 'true'
    else
        echo "No matching prefix found."
        return
    endif

    execute "normal ciw" . l:new_word
endfunction

" switch#NormalizedCase
" switch#Words
" switch#NormalizedCaseWords
let g:switch_custom_definitions =
            \ [
            \   switch#NormalizedCase(['one', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', 'nine', 'ten']),
            \   switch#Words(['isEmpty', 'isNotEmpty']),
            \   switch#Words(['isNull', 'isNotNull']),
            \   switch#Words(['isFalse', 'isTure']),
            \   switch#Words(['isSuccess', 'isFail']),
            \ ]


" 向下/向上移动到当前列存在字符的最后一行
function! MoveToLastValidColumn(direction)
    let target_col = virtcol('.')
    let current_line = line('.')
    let extra_cols = v:count1 - 1
    
    if a:direction ==# 'down'
        let last_line = line('$')
        while current_line < last_line
            let next_line = current_line + 1
            if virtcol([next_line, '$']) - 1 < target_col
                break
            endif
            let current_line = next_line
        endwhile
    else  " up
        while current_line > 1
            let prev_line = current_line - 1
            if virtcol([prev_line, '$']) - 1 < target_col
                break
            endif
            let current_line = prev_line
        endwhile
    endif

    let lines_move = abs(current_line - line('.'))
    
    " 普通模式直接跳转
    if mode() ==# 'n'
        call cursor(current_line, 0)
        exe "normal! " . target_col . "|"
        return
    endif
    
    " 可视模式/操作符模式：保持模式并移动
    if lines_move > 0
        let move_key = a:direction ==# 'down' ? 'j' : 'k'
        exe "normal! " . lines_move . move_key
    endif
    if extra_cols > 0
        exe "normal! " . extra_cols . "l"
    endif
endfunction


" Tableize
fun! CsvRowToList(...)
    let line = substitute(get(a:000, 0, getline('.')), '^\s\+|\s\+$', '', 'g')
    let len  = strlen(line) - 1

    if (len < 1) | return [] | endif

    let quote    = ""
    let escaped  = 0
    let currcol  = ""
    let result   = []

    for idx in range(0, len)
        let char = line[idx]
        if (escaped)
            let currcol .= char
            let escaped  = 0
        elseif (char == "\\")
            let escaped = 1
        elseif (!empty(quote))
            if (char != quote)
                let currcol .= char
            else
                let quote = ""
            endif
        elseif ((char == "'") || (char == "\""))
            let quote = char
        elseif (char == "\t")
            call add(result, currcol)
            let currcol = ""
        else
            let currcol .= char
        endif
    endfor

    if (!empty(currcol))
        call add(result, currcol)
    endif

    return result
endfun


fun! AlignString(str, align, length)
    let remaining = a:length - strwidth(a:str)

    if (remaining < 0) | return a:str[0:(a:length - 1)] | endif

    let center = !((a:align == 'right') || (a:align == 'left'))
    let lrem   = center ? float2nr(floor(remaining / 2.0)) : (a:align == 'left' ? 0 : remaining)
    let rrem   = center ? float2nr(ceil(remaining / 2.0))  : (remaining - lrem)

    return repeat(' ', lrem) . a:str . repeat(' ', rrem)
endfun


fun! InsertLine(line, position)
    let _z = @z
    let @z = a:line

    call cursor(a:position, 1)
    normal! A"zp

    let @z = _z
endfun


fun! TableRowToList(line)
    return map(split(substitute(a:line, '^\s\+\|\s\+$', '', 'g'),
                \ '|'),
                \ {idx, ln -> ln})
    " 字段加双引号
    " \ {idx, ln -> substitute(ln, '"', '\\"', 'g')})
endfun


fun! MaybeRestoreVisual()
    normal! gv
endfun

" 将excel转为markdown table
fun! Tableize() range
    let table_divider = '|'
    let next_nonblank = nextnonblank(a:firstline)
    let firstline     = getline(next_nonblank)
    let lines                                   = getline(a:firstline, a:lastline)
    let [col_maxlen, col_align, col_idx, parts] = [{}, {}, [], []]
    let [linecount, ld]                         = [range(0, len(lines) - 1), ' ' . table_divider . ' ']

    if (match(firstline, '[\t]') > -1) " CSV data found, convert it to a table
        for column in CsvRowToList(firstline)
            call add(col_idx, column)
            " left right center
            let col_align[column] = 'right'
        endfor

        for idx in linecount
            let lines[idx] = CsvRowToList(lines[idx])

            for column in range(0, len(lines[idx]) - 1)
                let curr_word_max = strwidth(lines[idx][column])
                let last_col_max  = get(col_maxlen, column, 0)

                if (curr_word_max > last_col_max) | let col_maxlen[column] = curr_word_max | endif
            endfor
        endfor

        for linec in linecount
            if !empty(filter(copy(lines[linec]), {idx, val -> !empty(val)}))
                let tmp = map(range(0, len(col_idx) - 1), {idx, val -> get(lines[linec], idx, '')})
                if linec == 0
                    call setline(a:firstline + linec,
                                \ ld[1:2] . join(map(tmp, {key, val -> AlignString(val,  'center', col_maxlen[key])}), ld) . ld[0:1])
                else
                    call setline(a:firstline + linec,
                                \ ld[1:2] . join(map(tmp, {key, val -> AlignString(val, get(col_align, get(col_idx, key, ''), 'center'), col_maxlen[key])}), ld) . ld[0:1])
                endif
            endif
        endfor

        for column in keys(col_maxlen)
            let align = tolower(get(col_align, get(col_idx, column, ''), 'left'))
            let lhs   = index(['right', 'center'], align) ? ':' : '-'
            let rhs   = index(['left',  'center'], align) ? ':' : '-'

            call add(parts, lhs . repeat('-', col_maxlen[column]) . rhs)
        endfor

        call InsertLine(table_divider . join(parts, table_divider) . table_divider, next_nonblank)
    elseif (match(firstline, '\%(|.*\)\{-2,}') > -1) " markdown table found, convert to CSV
        for idx in linecount
            let npos = a:firstline + idx

            if ((npos != next_nonblank + 1) && !empty(getline(npos)))
                let tmp = join(map(TableRowToList(lines[idx]),
                            \ {idx, col -> substitute(col, '^\s\+\|\s\+$', '', 'g')}), '	')

                " 字段加双引号
                " \ {idx, col -> substitute(col, '^\s\+\|\s\+$', '', 'g')}), '"	"')

                " 字段加双引号
                " call setline(npos, '"' . tmp . '"')
                call setline(npos, tmp)
            endif
        endfor

        exe ((next_nonblank + 1) . "d_")
        call cursor(a:lastline - 1, 1)
    endif
endfun

" Tableize end

